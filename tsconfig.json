{
  "compilerOptions": {
    "baseUrl": "src", // Set the base URL to the src directory
    "lib": ["es2016"],
    "paths": {
      "@/*": ["*"] // Optional: Map '@/' to the src directory
    }
  }
}
